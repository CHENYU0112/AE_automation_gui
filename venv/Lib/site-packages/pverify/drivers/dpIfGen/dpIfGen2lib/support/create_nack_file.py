from __future__ import annotations

from os.path import abspath, dirname, normpath

from dpIfGen2lib.support.flow_nak_msgs_to_dict import NakInfoParser

this_path = dirname(abspath(__file__))
file_in = normpath("".join([this_path, "./../test/dp3/support/nack_codes.h"]))
file_out = normpath("".join([this_path, "./../dpIfGen3API/nak_codes.py"]))
nip = NakInfoParser(file_in)
nip.infos.save(file_out)
print("created file %s" % file_out)
