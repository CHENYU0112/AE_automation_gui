from __future__ import annotations


def enum_from_value(enum, value):
    for item in enum:
        if item.value == value:
            return item
    msg = f"There is no enum with value '{value}'."
    raise IndexError(msg)
